import React from 'react'
import { string, number } from 'prop-types';

export const Challenge = ({id=0, UserThrows=0, OppThrows=0,Goal=0,TotalUserThrows=0,TotalOppThrows=0}) => {
    const lead = UserThrows - OppThrows
    let grid = Array(Goal+1)

	return (
		<div>
            
		</div>
	)
}

